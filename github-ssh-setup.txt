ssh-keygen -t ed25519 -C "chris.teli@gmail.com"

On GitHub

- In the upper-right corner of any page on GitHub, click your profile picture, then click  Settings.

- In the "Access" section of the sidebar, click  SSH and GPG keys.

- Click New SSH key or Add SSH key.

- - In the "Title" field, add a descriptive label for the new key. For example, if you're using a personal laptop, you might call this key "Personal laptop".

- - Select the type of key, either authentication or signing. For more information about commit signing, see About commit signature verification.

- - In the "Key" field, paste your public key.

- - Click Add SSH key.

- - If prompted, confirm access to your account on GitHub. For more information, see Sudo mode.

Change remote URL to use ssh format if already cloned

git remote set-url origin git@github.com:moistvonlipwig/my-dev-essentials.git 

To clone a new repository
- git clone git@github.com:moistvonlipwig/cteli-itsec.git
